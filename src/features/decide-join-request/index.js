/**
 * Public surface of the `decide-join-request` feature (#1109). The
 * `join-requests` widget composes it from this index only, never from the
 * component file directly.
 */
export { default } from './ui/DecideJoinRequest';
