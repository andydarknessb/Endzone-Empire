/**
 * Public surface of the `set-confidence` feature (#1265, ADR 0038): the
 * per-card Confidence menu. The widget composes it from here only, never
 * from the component file directly.
 */
export { default } from './ui/ConfidenceMenu';
export { default as ConfidenceMenu } from './ui/ConfidenceMenu';
