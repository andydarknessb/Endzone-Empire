/**
 * Public surface of the `copy-invite` feature (ADR 0020). The page composes it
 * from this index only, never from the component file directly.
 */
export { default } from './CopyInvite';
export { joinLink } from './CopyInvite';
