/**
 * Public surface of the `advance-week` feature (ADR 0020). The commissioner
 * panel composes it from this index only, never from the component file
 * directly.
 */
export { default } from './ui/AdvanceWeek';
