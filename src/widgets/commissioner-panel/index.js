/**
 * Public surface of the commissioner-panel widget (League Dashboard rail,
 * ADR 0020 / ticket #644). The page composes it from here; everything else in
 * this folder is the widget's own internal slice.
 */
export { default } from './ui/CommissionerPanel';
