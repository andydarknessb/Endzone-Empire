/**
 * Public surface of the recent-activity widget (League Dashboard rail card,
 * ADR 0020 / ticket #1105). The page composes it from here; everything else
 * in this folder is the widget's own internal slice.
 */
export { default } from './ui/RecentActivity';
