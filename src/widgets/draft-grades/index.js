/**
 * Public surface of the draft-grades widget (League Dashboard rail-top,
 * ADR 0020 / ticket #642). The page composes it from here; everything else in
 * this folder is the widget's own internal slice.
 */
export { default } from './ui/DraftGrades';
