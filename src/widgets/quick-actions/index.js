/**
 * Public surface of the quick-actions widget (League Dashboard, ADR 0020 /
 * ticket #643). The page composes it from here; everything else in this folder
 * is the widget's own internal slice.
 */
export { default } from './ui/QuickActions';
