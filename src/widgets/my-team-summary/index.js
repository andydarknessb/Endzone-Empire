/**
 * Public surface of the my-team-summary widget (League Dashboard hero-left,
 * ADR 0020 / ticket #639). The page composes it from here; everything else in
 * this folder is the widget's own internal slice.
 */
export { default } from './ui/MyTeamSummary';
