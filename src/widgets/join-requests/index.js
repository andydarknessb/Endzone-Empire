/**
 * Public surface of the `join-requests` widget (commissioner-console,
 * console-only, #1109). The console page composes it from this index only,
 * never from the component file directly.
 */
export { default } from './ui/JoinRequests';
