/**
 * Public surface of the `league-dashboard` page slice (ADR 0020). The router
 * (at the cutover ticket) and this slice's own test compose it from this index
 * only, never from the component file directly.
 */
export { default } from './LeagueDashboardPage';
