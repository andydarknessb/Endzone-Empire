/**
 * Public surface of the Waivers page slice (ADR 0020/0049, #1613). Mounted at
 * `/league/:leagueId/waivers` (App.jsx); the legacy `src/components/WaiverWire`
 * it replaces is deleted in this same ticket.
 */
export { default } from './WaiversPage';
