/**
 * Public surface of the Lineup page slice (ADR 0020/0037, #1237). Mounted at
 * `/team` (App.jsx); the legacy `src/components/LineupScreen` it replaces is
 * deleted in this same ticket.
 */
export { default } from './LineupPage';
