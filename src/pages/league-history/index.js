/**
 * Public surface of the `league-history` page slice (ADR 0020/0029
 * precedent). The router and this slice's own test compose it from this
 * index only, never from the component file directly.
 */
export { default } from './LeagueHistoryPage';
