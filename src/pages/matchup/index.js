/**
 * Public surface of the `matchup` page slice (ADR 0031, #903). The router
 * (src/components/App/App.jsx) and this slice's own test compose it from this
 * index only, never from the component file directly.
 */
export { default } from './MatchupPage';
