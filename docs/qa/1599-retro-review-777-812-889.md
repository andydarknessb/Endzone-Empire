# Retroactive review: PRs #777, #812 and #889 (issue #1599)

The three PRs merged into `integration` with no review on GitHub (ledger finding `merged-without-review`). Each was read at its merged head and re-read on `integration` at `c25ba7a1` (the code has moved since). Real defects are filed as their own `bug` issues; no product code changes under #1599.

## #777: PickClock Overdue copy (#769), merged head `1327fa532f6dd59fc595b2e13c6c808ea3a81b46`

Examined at `integration` (`c25ba7a1`): `src/components/DraftBoard/PickClock.jsx`, `src/components/DraftBoard/LiveDraftBanner.jsx`, `src/hooks/useCountdownTicking.js`; the merged diff of `src/lib/onTheClock.js` (now `src/shared/lib/onTheClock.js`) and the parity test.

Findings: none. The one candidate, the Overdue line shown twice (banner and leaf), was already found and fixed by #844 (the banner copy is now visually hidden). The other candidate, a single stale render of the outgoing turn's Overdue state on a deadline swap, is cosmetic and not filed.

## #812: Draft assistant room presenter (#787), merged head `67e6d47aca1edfdaf897f380bf0ac04c56d5417f`

Examined at `integration` (`c25ba7a1`): `src/components/DraftBoard/roomAssistantFacts.js` (whole file), `src/components/DraftBoard/DraftRoomAssistant.jsx` (pool ref, Net vs ADP, urgent edge and the own-pick and Queue snipe effect, lines 160 to 270), the `LiveDraftBanner.jsx` / `PickClock.jsx` urgent-edge wiring, and the merged diff of `DraftBoard.jsx` (provider props, `handleSelectFromPool`, region and toggle mounts), `DraftRail.jsx` (assistant panel entry), `DraftStatusBar.jsx` (toggle slot) and `railComposition.js` (`ASSISTANT` in the active rail). Those four are small wiring changes and read as correct.

Out of scope, with reasons: the rest of `DraftRoomAssistant.jsx` (browse effect, panel and region rendering) was not read, and the test files and `DraftPresenter.test.jsx` were not read (tests only; the presenter itself is untouched by the PR).

Findings: one, filed as #1601 (minor, correctness: own-pick and Queue-snipe labels read ADP from the windowed pool row while Net vs ADP reads `pick.adp`).

## #889: one pricing rule and one live subscription per Matchup (#883, #885), merged head `de62d9277358f59cc84be55a5e09b54b506d0c2b`

Examined at `integration` (`c25ba7a1`): the merged diff of `server/services/expectedFinal.service.js` and its current status derivation (`statusForMatchup`, empty-starters guard), and `server/routes/league.router.js` (detail route pricing).

The #885 half no longer exists in its merged form: `useLiveGames` in `useMatchup.js` became `src/entities/matchup/model/useLiveGameStates.js` (one channel over every listed game, scheduled included), `LiveGameStatus.jsx` was removed (#909), and `src/hooks/useLiveGameRealtime.js` was deleted by #889 itself. So that half was read as it stands today: `useLiveGameStates.js`; its callers `useMatchup.js` and `useLeagueMatchups.js` (id set deduped by a string key, so a silent refetch does not resubscribe); the consumers `src/pages/matchup/MatchupPage.jsx` and `src/pages/game-center/GameCenterPage.jsx` (through `useMatchupPage.js` and `useGameCenter.js`); and the live-status UI that replaced `LiveGameStatus.jsx`: `src/widgets/nfl-game-strip/` (`NflGameStrip.jsx`, `gameTileView.js`) and `src/widgets/retro-scoreboard/ui/GamesTile.jsx`. `src/components/MatchupDetail/*` no longer exists (ADR 0031), so its changes were read at the merged head only. Not read: the rest of `retro-scoreboard` (`LedBoard`, `LineupsCard`, `RetroField`, `scoreboardModel.js`), which open no live-game subscription.

Findings: one, filed as #1603 (nit, correctness: an UPDATE between the initial read and the channel join is lost). The pricing path has no findings: bench-only teams get a null-figure entry without flipping status, and best-ball rows ride in both `starters` and `bench` as documented. `NflGameStrip` and `GamesTile` are pure renders of the rows they are handed and open no channel.
